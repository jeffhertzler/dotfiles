local catppuccin = require("colors/catppuccin").setup({})

return {
  colors = catppuccin,
  hide_tab_bar_if_only_one_tab = true,
  use_ime = false,
  window_padding = {
    left = 0,
    right = 0,
    top = 0,
    bottom = 0,
  },
}
