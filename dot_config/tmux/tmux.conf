# remap prefix from 'C-b' to 'C-a'
unbind C-b
set -g prefix C-a
bind C-a send-prefix

# start at 1
set -g base-index 1

# shift windows
bind C-h swap-window -t -1\; select-window -t -1
bind C-l swap-window -t +1\; select-window -t +1

# split panes
bind M-h split-window -bh
bind M-l split-window -h
bind M-k split-window -bv
bind M-j split-window -v
unbind '"'
unbind %

# reload config file
bind R source-file ~/.config/tmux/tmux.conf

# reload pane
bind P respawn-pane -k

bind h select-window -t -1
bind l select-window -t +1

# Enable mouse control (clickable windows, panes, resizable panes)
set -g mouse on

# vim bindings
set -g mode-keys vi

# Fix vim colors
set -g default-terminal "tmux-256color"
set -ga terminal-overrides ',alacritty:RGB'
# set -ga terminal-overrides ",xterm-256color:Tc"

set -g renumber-windows on

set -g @continuum-restore 'on'
# set -g @continuum-boot 'on'

set -g status-position top

TMUX_FZF_LAUNCH_KEY="C-a"

# set -g @dracula-show-powerline true
# set -g @dracula-show-flags true

###########################
# Plugins
###########################
# To install plugins:
#   1) Add plugin down here
#   2) Prefix + I to install plugin
# To update plugins:
#   1) Prefix + U
# To remove plugins:
#   1) Remove line down here
#   2) Prefix + ALT + U

set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-sensible'
# set -g @plugin 'dracula/tmux'
# set -g @plugin 'logico-dev/tokyo-night-tmux'
# set -g @plugin 'jabirali/tmux-minimono'
# set -g @plugin "arcticicestudio/nord-tmux"
# set -g @plugin "catppuccin/tmux"
# set -g @plugin 'christoomey/vim-tmux-navigator'
set -g @plugin 'tmux-plugins/tmux-resurrect'
set -g @plugin 'tmux-plugins/tmux-continuum'
set -g @plugin 'tmux-plugins/tmux-yank'
set -g @plugin 'tmux-plugins/tmux-sessionist'
set -g @plugin 'sainnhe/tmux-fzf'

run -b '~/.config/tmux/plugins/tpm/tpm'

# set -g status-style fg=white,bg=black
# set -g mode-style fg=white,bg=black
# set -g pane-border-style fg=black,bg=default
# set -g pane-active-border-style fg=black,bg=default
# set -g message-style fg=white,bg=black
# set -g message-command-style fg=white,bg=black

# Smart pane switching with awareness of vim and fzf
forward_programs="view|n?vim?|fzf"

should_forward="ps -o state= -o comm= -t '#{pane_tty}' \
  | grep -iqE '^[^TXZ ]+ +(\\S+\\/)?g?($forward_programs)(diff)?$'"

bind -n C-h if-shell "$should_forward" "send-keys C-h" "select-pane -L"
bind -n C-j if-shell "$should_forward" "send-keys C-j" "select-pane -D"
bind -n C-k if-shell "$should_forward" "send-keys C-k" "select-pane -U"
bind -n C-l if-shell "$should_forward" "send-keys C-l" "select-pane -R"
bind -n C-\\ if-shell "$should_forward" "send-keys C-\\" "select-pane -l"

# catppuccin
# NOTE: you can use vars with $<var> and ${<var>} as long as the str is double quoted: ""
# WARNING: hex colors can't contain capital letters

# --> Catppuccin
thm_mocha_rosewater="#f5e0dc"
thm_mocha_flamingo="#f2cdcd"
thm_mocha_pink="#f5c2e7"
thm_mocha_mauve="#cba6f7"
thm_mocha_red="#f38ba8"
thm_mocha_maroon="#eba0ac"
thm_mocha_peach="#fab387"
thm_mocha_yellow="#f9e2af"
thm_mocha_green="#a6e3a1"
thm_mocha_teal="#94e2d5"
thm_mocha_sky="#89dceb"
thm_mocha_sapphire="#74c7ec"
thm_mocha_blue="#89b4fa"
thm_mocha_lavender="#b4befe"
thm_mocha_text="#cdd6f4"
thm_mocha_subtext1="#bac2de"
thm_mocha_subtext0="#a6adc8"
thm_mocha_overlay2="#9399b2"
thm_mocha_overlay1="#7f849c"
thm_mocha_overlay0="#6c7086"
thm_mocha_surface2="#585b70"
thm_mocha_surface1="#45475a"
thm_mocha_surface0="#313244"
thm_mocha_base="#1e1e2e"
thm_mocha_mantle="#181825"
thm_mocha_crust="#11111b"

# original
# thm_bg="#1e1e28"
# thm_fg="#dadae8"
# thm_cyan="#c2e7f0"
# thm_black="#15121c"
# thm_gray="#332e41"
# thm_magenta="#c6aae8"
# thm_pink="#e5b4e2"
# thm_red="#e38c8f"
# thm_green="#b1e3ad"
# thm_yellow="#ebddaa"
# thm_blue="#a4b9ef"
# thm_orange="#f9c096"
# thm_black4="#575268"

thm_bg="${thm_mocha_base}"
thm_fg="${thm_mocha_text}"
thm_cyan="${thm_mocha_sky}"
thm_black="${thm_mocha_crust}"
thm_gray="${thm_mocha_surface0}"
thm_magenta="${thm_mocha_mauve}"
thm_pink="${thm_mocha_pink}"
thm_red="${thm_mocha_red}"
thm_green="${thm_mocha_green}"
thm_yellow="${thm_mocha_yellow}"
thm_blue="${thm_mocha_blue}"
thm_orange="${thm_mocha_peach}"
thm_black4="${thm_mocha_mantle}"

# ----------------------------=== Theme ===--------------------------

# status
set -g status-position top
set -g status on
set -g status-bg "${thm_bg}"
set -g status-justify "left"
set -g status-left-length "100"
set -g status-right-length "100"

# messages
set -g message-style fg="${thm_cyan}",bg="${thm_gray}",align="centre"
set -g message-command-style fg="${thm_cyan}",bg="${thm_gray}",align="centre"

# panes
set -g pane-border-style fg="${thm_gray}"
set -g pane-active-border-style fg="${thm_blue}"

# windows
setw -g window-status-activity-style fg="${thm_fg}",bg="${thm_bg}",none
setw -g window-status-separator ""
setw -g window-status-style fg="${thm_fg}",bg="${thm_bg}",none

# --------=== Statusline

set -g status-left ""

# original
# set -g status-right "#[fg=$thm_pink,bg=$thm_bg,nobold,nounderscore,noitalics]#[fg=$thm_bg,bg=$thm_pink,nobold,nounderscore,noitalics] #[fg=$thm_fg,bg=$thm_gray] #W #{?client_prefix,#[fg=$thm_red],#[fg=$thm_green]}#[bg=$thm_gray]#{?client_prefix,#[bg=$thm_red],#[bg=$thm_green]}#[fg=$thm_bg] #[fg=$thm_fg,bg=$thm_gray] #S "

set -g status-right "#[fg=$thm_mocha_maroon,bg=$thm_bg,nobold,nounderscore,noitalics]#[fg=$thm_bg,bg=$thm_mocha_maroon,nobold,nounderscore,noitalics] #[fg=$thm_bg,bg=$thm_mocha_maroon] #W #{?client_prefix,#[fg=$thm_red],#[fg=$thm_mocha_flamingo]}#[bg=$thm_mocha_maroon]#{?client_prefix,#[bg=$thm_red],#[bg=$thm_mocha_flamingo]}#[fg=$thm_bg] #{?client_prefix,#[bg=$thm_red],#[bg=$thm_mocha_flamingo]}#[fg=$thm_bg] #S "
set -g status-format[1] "#[fill=$thm_bg]"

# current_dir
# original
# setw -g window-status-format "#[fg=$thm_bg,bg=$thm_blue] #I #[fg=$thm_fg,bg=$thm_gray] #W "
# setw -g window-status-current-format "#[fg=$thm_bg,bg=$thm_orange] #I #[fg=$thm_fg,bg=$thm_bg] #W "

# setw -g window-status-format "#[bg=$thm_blue,fg=$thm_gray]#[fg=$thm_gray,bg=$thm_blue] #I #[bg=$thm_gray,fg=$thm_blue]#[fg=$thm_fg,bg=$thm_gray] #W "
# setw -t :1 window-status-format "#[fg=$thm_gray,bg=$thm_blue] #I #[bg=$thm_gray,fg=$thm_blue]#[fg=$thm_fg,bg=$thm_gray] #W"
# setw -g window-status-current-format "#[fg=$thm_gray,bg=$thm_orange]#[fg=$thm_gray,bg=$thm_orange] #I #[bg=$thm_gray,fg=$thm_orange]#[fg=$thm_fg,bg=$thm_gray] #W "
# setw -t :1 window-status-current-format "#[fg=$thm_gray,bg=$thm_orange] #I #[bg=$thm_gray,fg=$thm_orange]#[fg=$thm_fg,bg=$thm_gray] #W"

set -g window-status-format "#[bg=$thm_blue,fg=$thm_bg]#[fg=$thm_bg,bg=$thm_blue] #I #[bg=$thm_bg,fg=$thm_blue]#[fg=$thm_fg,bg=$thm_bg] #W "
set -t :1 window-status-format "#[fg=$thm_bg,bg=$thm_blue] #I #[bg=$thm_bg,fg=$thm_blue]#[fg=$thm_fg,bg=$thm_bg] #W "
set -g window-status-current-format "#[fg=$thm_bg,bg=$thm_orange]#[fg=$thm_bg,bg=$thm_orange] #I #[bg=$thm_bg,fg=$thm_orange]#[fg=$thm_fg,bg=$thm_bg] #W "
set -t :1 window-status-current-format "#[fg=$thm_bg,bg=$thm_orange] #I #[bg=$thm_bg,fg=$thm_orange]#[fg=$thm_fg,bg=$thm_bg] #W "

# setw -g window-status-format "#[bg=$thm_blue,fg=$thm_black4]#[fg=$thm_black4,bg=$thm_blue] #I #[bg=$thm_black4,fg=$thm_blue]#[fg=$thm_fg,bg=$thm_black4] #W "
# setw -t :1 window-status-format "#[fg=$thm_black4,bg=$thm_blue] #I #[bg=$thm_black4,fg=$thm_blue]#[fg=$thm_fg,bg=$thm_black4] #W"
# setw -g window-status-current-format "#[fg=$thm_black4,bg=$thm_orange]#[fg=$thm_black4,bg=$thm_orange] #I #[bg=$thm_black4,fg=$thm_orange]#[fg=$thm_fg,bg=$thm_black4] #W "
# setw -t :1 window-status-current-format "#[fg=$thm_black4,bg=$thm_orange] #I #[bg=$thm_black4,fg=$thm_orange]#[fg=$thm_fg,bg=$thm_black4] #W"

# parent_dir/current_dir
# setw -g window-status-format "#[fg=colour232,bg=colour111] #I #[fg=colour222,bg=colour235] #(echo '#{pane_current_path}' | rev | cut -d'/' -f-2 | rev) "
# setw -g window-status-current-format "#[fg=colour232,bg=colour208] #I #[fg=colour255,bg=colour237] #(echo '#{pane_current_path}' | rev | cut -d'/' -f-2 | rev) "

# --------=== Modes
setw -g clock-mode-colour "${thm_blue}"
setw -g mode-style "fg=${thm_pink} bg=${thm_black4} bold"
